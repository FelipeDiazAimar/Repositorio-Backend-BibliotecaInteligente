// Este archivo está preparado para definir rutas relacionadas con las "respuestas" (de la IA o del sistema).
// Por ahora está vacío, pero aquí puedes agregar rutas como GET, POST, etc. para respuestas.

const express = require('express');
const router = express.Router();

// Ejemplo de cómo podrías agregar una ruta en el futuro:
// router.post('/', respuestaController.create);

module.exports = router;