// Este archivo está preparado para definir rutas relacionadas con los "prompts" (mensajes enviados a la IA).
// Por ahora está vacío, pero aquí puedes agregar rutas como GET, POST, etc. para prompts.

const express = require('express');
const router = express.Router();

// Ejemplo de cómo podrías agregar una ruta en el futuro:
// router.post('/', promptController.create);

module.exports = router;